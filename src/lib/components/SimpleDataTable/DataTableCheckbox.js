import { withStyles } from '@material-ui/core';
import Checkbox from '@material-ui/core/Checkbox/Checkbox';

export const DataTableCheckbox = withStyles({
  root: {
    padding: '0',
  },
})(Checkbox);
