module.exports = () =>
  `// import { take, call, put, select } from 'redux-saga/effects';
  //let restInstance;

// Individual exports for testing
export default function* {{ camelCase crudModulePrefix }}EditSaga(/* { getMethod , postMethod, customMethod } */) {
  // See example in containers/MyBusiness/TreeView/saga.js
}
`;
