/*
 * BusinessGenerator Messages
 *
 * This contains all the text for the BusinessGenerator component.
 */

const { defineMessages } = require('react-intl');

export const scope = 'app.components.BusinessGenerator';

export default defineMessages({
  add_a_business: {
    id: `${scope}.addABussiness`,
    defaultMessage: 'Add a business',
  },
});
