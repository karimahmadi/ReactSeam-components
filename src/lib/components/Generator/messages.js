/*
 * Generator Messages
 *
 * This contains all the text for the Generator component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Generator';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Generator component!',
  },
});
